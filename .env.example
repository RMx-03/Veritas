# Frontend Configuration (React/Vite)
# This file should be renamed to .env and placed in the root directory

# Backend API URL (where your FastAPI server runs)
VITE_API_URL=http://localhost:8000

# Production URLs (for deployment)
VITE_FRONTEND_URL=https://your-app.vercel.app
VITE_BACKEND_URL=https://your-app.deta.dev

# Note: All backend configuration (Groq, Supabase, OCR) 
# should go in backend/.env, not here!
