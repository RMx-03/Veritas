# AI Services
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
# OpenRouter (LLM via Chat Completions)
# Backend uses OpenRouter (DeepSeek R1) for AI analysis.
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Pick a model from https://openrouter.ai/models (examples below)
OPENROUTER_MODEL=deepseek/deepseek-r1
# Optional: used for HTTP Referer and X-Title headers (best practice)
OPENROUTER_SITE_URL=http://localhost:8000
OPENROUTER_APP_NAME=Veritas Local

# Database
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# External APIs (Optional)
USDA_API_KEY=your_usda_api_key_here

# OCR Engine (backend)
# Options: trocr | donut
OCR_ENGINE=trocr

# Hugging Face model overrides (optional)
# TrOCR models: microsoft/trocr-large-printed, microsoft/trocr-base-printed
HF_TROCR_MODEL=microsoft/trocr-large-printed
# Donut models: naver-clova-ix/donut-base-finetuned-docvqa or task-specific fine-tunes
HF_DONUT_MODEL=naver-clova-ix/donut-base-finetuned-docvqa

# Optional Layout refinement using DocVQA (LayoutLMv3 family)
USE_LAYOUT_REFINER=false
HF_DOCVQA_MODEL=impira/layoutlm-document-qa

# CORS Origins (for production)
ALLOWED_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# Tesseract OCR (optional, improves fallback OCR)
# On Windows, set to the installed Tesseract path, e.g.:
# TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe
# On macOS (with brew):
# TESSERACT_CMD=/opt/homebrew/bin/tesseract
# On Linux, usually on PATH so leave unset
